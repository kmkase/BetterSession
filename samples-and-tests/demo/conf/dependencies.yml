# Application dependencies

require:
    - play
#    - play -> secure

    - bettersession -> bettersession

repositories:
    - test improvedsession module:
        type:       local
        artifact:   ${application.path}/../../../[module]
        contains:
            - bettersession
